mixin section__sales-block()
   :php
   - tmp1 = "<?= fi(63) ?>"
   .section__sales-block.abs.top0.right0.tac.tdn.cup&attributes(attributes)
      a(href!=tmp1).cup.tdn
         - let i = -1, D = 150, R = D/2;
         - let R2 = R**2, total = 20, padding = 3, middle = total / 2, step = D / total;
         - const EM = 14;
         while i++ < total
            - let wi = R - (R2 - ((i - middle)*step + step/2)**2)**0.5
            - if (wi + padding < R) wi += padding;
            .section__sales-block-round-fixer(style=`width:${wi/EM}em;height:${step/EM}em`)
            .section__sales-block-round-fixer(style=`width:${wi/EM}em;height:${step/EM}em`)
         - tmp = "<?= fi(64) ?>"
         .mto25 !{tmp}
         a.abs.bottom1o25.ct-abs_horiz.fwb(href!=tmp1) Подробнее